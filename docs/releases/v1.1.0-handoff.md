# CyberSentinel AI v1.1.0 — Final Handoff

- Date: 2026-09-05
- Release: `v1.1.0`
- Release candidate: `v1.1.0-rc.1`

## Outcome

Phase M is complete when the final tag resolves to the state-bearing commit and
GitHub CI/Security plus the no-mock public smoke pass on that commit. The release
contains the complete official sequence `G → H → I → J → K → L → M`; completed
phases were not replayed except for regression verification.

## Final Release Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| Clean Git tree | PASS | `git status --porcelain` empty before tag |
| Fresh clone/check-out | PASS | GitHub-hosted jobs check out only tracked files; static release gate runs first |
| One-command startup | PASS | `scripts/start-demo.sh`; generated gitignored secrets, Compose build, migration, seed, readiness |
| Fresh DB migration | PASS | CI PostgreSQL 16 empty DB → Alembic head, downgrade/re-upgrade, schema + CRUD |
| Full E2E | PASS | Frontend lint/build and complete Playwright suite |
| Security scan | PASS | pip-audit, npm audit, Bandit, Redis integration, Trivy, OWASP ZAP |
| Backup/restore | PASS | checksum dump restored into disposable DB; migration and table count matched |
| Staging/public smoke | PASS | hosted Render + Neon no-mock journey across all primary reviewer pages |
| Final tag | TAG-TIME | annotated `v1.1.0` is created only after every prior gate is green |
| Release notes | PASS | `docs/releases/v1.1.0.md` and `CHANGELOG.md` |
| Final handoff | PASS | this document |
| State JSON | PASS | `docs/releases/v1.1.0-state.json` validates in CI |

## Public runtime

- Website: <https://cybersentinel-web-ppae.onrender.com>
- API readiness: <https://cybersentinel-api-hrl8.onrender.com/ready>
- Hosting: Render Free Web Services, Singapore
- Database: Neon Free PostgreSQL, Singapore
- Demo data: 8 synthetic events, 3 incidents, restricted Analyst account
- Public API docs: disabled; registration: disabled

## Evidence sources

- CI: <https://github.com/ngtd-2609/cybersentinel-ai/actions/workflows/ci.yml>
- Security: <https://github.com/ngtd-2609/cybersentinel-ai/actions/workflows/security.yml>
- Release: <https://github.com/ngtd-2609/cybersentinel-ai/releases/tag/v1.1.0>
- Phase K model/RAG report: `reports/phase_k_ai_reliability.json`
- Phase L public evidence: `docs/sre/phase-l-evidence.md`

The tagged commit is the authoritative identity. Run
`git rev-list -n 1 v1.1.0` to resolve it; the state JSON deliberately uses this
immutable reference instead of embedding a self-referential commit hash.

## Operational notes

- Render Free cold starts of roughly one minute are expected and explained in the
  login UI.
- Neon PostgreSQL is authoritative; Render filesystems are ephemeral.
- Reseed is idempotent and must receive the database URL/password only through a
  trusted environment.
- Self-hosted staging/SRE tools remain optional evidence, not a paid-VPS
  dependency.
- A 5–8 minute video is optional and deferred; it does not block this release.

## Next owner actions

No engineering action is required to use the portfolio. Periodically open the
public link, review provider free-tier quotas, and rotate the demo password if it
is ever exposed. If a video is later recorded, add it as a documentation-only
release update without changing the historical Phase M result.
